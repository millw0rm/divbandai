server {
    listen 80;
    server_name divbandai.ir www.divbandai.ir test.divbandai.ir;

    root /usr/share/nginx/html;
    index index.html;

    location = /healthz {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
