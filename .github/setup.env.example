# Copy to .github/setup.env (gitignored) and run:
#   scripts/setup-github-actions.sh -y --env-file .github/setup.env
#
# First on the VPS: sudo bash scripts/install-vps-deploy.sh

DIVBAND_DEPLOY_WEBHOOK_URL=http://94.101.178.146:9090/deploy
DIVBAND_DEPLOY_WEBHOOK_SECRET=replace-with-value-from-/etc/divband/deploy.env
DIVBAND_ARVAN_ENABLED=true

# Optional: PAT with read:packages (private GHCR on VPS + GitHub secret)
# DIVBAND_GHCR_TOKEN=ghp_...
