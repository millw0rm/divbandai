# Copy to .github/setup.env (gitignored) and run:
#   scripts/setup-github-actions.sh -y --env-file .github/setup.env --generate-key
#
# Do not commit setup.env — it may point at local key paths.

DIVBAND_VPS_HOST=94.101.178.146
DIVBAND_VPS_USER=ubuntu
DIVBAND_SSH_PRIVATE_KEY_FILE=${HOME}/.ssh/divband_github_actions
DIVBAND_ARVAN_ENABLED=true

# Optional: copy deploy public key to the VPS during setup
# DIVBAND_COPY_SSH_ID=1
