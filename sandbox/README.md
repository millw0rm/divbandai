# Sandbox

This directory is reserved for drafts, experiments, and unfinished ideas.

Rules:

- Nothing in this directory is deployed automatically.
- Do not store production secrets here.
- Drafts should graduate into `docs`, `apps`, `infra`, or `demo` before release.
- Experiments should clearly state assumptions and cleanup requirements.
