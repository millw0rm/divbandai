{
  "name": "@divband/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Static dashboard frontend for creating and operating divband projects.",
  "type": "module",
  "main": "src/dashboard.ts",
  "scripts": {
    "dev": "npm run build && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs"
  }
}
